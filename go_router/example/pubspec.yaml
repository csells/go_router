name: go_router_examples
description: go_router examples
version: 2.5.2
publish_to: none

environment:
  sdk: ">=2.14.0 <3.0.0"

dependencies:
  adaptive_navigation: ^0.0.4
  collection: ^1.15.0
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  go_router: ^3.0.0-0
  provider: ^5.0.0
  url_launcher: ^6.0.7

dev_dependencies:
  all_lint_rules_community: ^0.0.4
  build_runner: ^2.0.0
  build_verify: ^3.0.0
  flutter_test:
    sdk: flutter
  go_router_builder: ^3.0.0-0
  test: ^1.17.0

flutter:
  uses-material-design: true

dependency_overrides:
  go_router:
    path: ../
  go_router_builder:
    path: ../../go_router_builder
